import "package:flutter/material.dart";

class ActScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Get to it"),
      ),
    );
  }
}
